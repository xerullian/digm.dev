import { users } from './users';
import { snippets } from './snippets';
import { tags } from './tags';
import { fileTypes } from './fileTypes';
import { snippetVersions } from './snippetVersions';

export const tables = {
  users,
  snippets,
  tags,
  fileTypes,
  snippetVersions
}
